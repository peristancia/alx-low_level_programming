#include "main.h"
/**
 * main - Entry point
 * Description: print the alphabets in lowercase
 */
void print_alphabet(void)
{
	char letter;

	for (letter = 'a'; letter <= 'z'; leter++)
		_putchar(letter);
	_putchar('\n');
}
